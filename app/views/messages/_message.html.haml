.main__chat__messages{data: {message: {id: message.id}}}
  .main__chat__messages__massege
    .main__chat___messages__massege__name
      = message.user.name
    .main__chat__messages__massege__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .main__chat__messages__text
    - if message.content.present?
      = message.content
  = image_tag message.image.url, class: 'message__text__image' if message.image.present?